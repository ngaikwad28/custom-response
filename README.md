# custom-response 


# custom-response for Kong

## Overview

The `custom-response` plugin is used to remove response headers which user dont want to displayfor Kong Gateway.
## Installation

1. **Clone the repository:**

   ```bash
   git clone https://github.com/ngaikwad28/custom-response.git
   cd custom-response

=========================================================================================
### 2. Install and Enable the Plugin

1. **Install the Plugin**

   If you have LuaRocks installed, navigate to the plugin directory and run:

   ```bash
   sudo luarocks make kong-plugin-custom-response-0.1.0-1.rockspec










